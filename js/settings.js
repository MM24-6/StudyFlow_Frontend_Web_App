document.addEventListener('DOMContentLoaded',()=>{const p=getPreferences();const set=(id,v)=>{const e=document.getElementById(id);if(e)e.value=v??''};set('profileName',p.name);set('profileEmail',p.email);set('profileProgram',p.program);set('weeklyGoal',p.weeklyGoal);document.getElementById('profileAvatar').textContent=p.avatar||'MM';document.getElementById('profileNamePreview').textContent=p.name||'Student';document.getElementById('notifyToggle').checked=p.notifications!==false;document.getElementById('emailToggle').checked=!!p.emailReminders;document.querySelectorAll('.settings-nav-item').forEach(b=>b.onclick=()=>{document.querySelectorAll('.settings-nav-item').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.settings-section').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.getElementById('section-'+b.dataset.section).classList.add('active')});document.getElementById('profileForm').onsubmit=e=>{e.preventDefault();const name=document.getElementById('profileName').value.trim(),email=document.getElementById('profileEmail').value.trim();if(!name||!StudyFlowApp.validateEmail(email)){StudyFlowApp.showToast('Please enter a valid name and email.','error');return}const prefs={...getPreferences(),name,email,program:document.getElementById('profileProgram').value.trim()||'Computer Science',weeklyGoal:Number(document.getElementById('weeklyGoal').value)||25,avatar:name.split(/\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase()};savePreferences(prefs);document.getElementById('profileAvatar').textContent=prefs.avatar;document.getElementById('profileNamePreview').textContent=prefs.name;StudyFlowApp.showToast('Profile saved successfully.','success')};document.getElementById('settingsTheme').onclick=()=>StudyFlowApp.toggleTheme();document.getElementById('saveNotifications').onclick=()=>{savePreferences({...getPreferences(),notifications:document.getElementById('notifyToggle').checked,emailReminders:document.getElementById('emailToggle').checked});StudyFlowApp.showToast('Notification preferences saved.','success')};StudyFlowApp.refreshIcons();});
